package java.math;

public enum RoundingMode {
	UP, DOWN, CEILING, FLOOR, HALF_UP, HALF_DOWN, HALF_EVEN, UNNECESSARY;

	public static RoundingMode valueOf(final int rm) {
		throw new RuntimeException("Stub!");
	}
}
